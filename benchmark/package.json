{
  "name": "sof-benchmark",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Implementation-agnostic SQL on FHIR benchmark artifact and reference materialization tool",
  "scripts": {
    "test": "bun test",
    "validate": "bun run tools/validate-benchmarks.js",
    "data": "bun run tools/cli.js",
    "check-fmt": "bunx prettier -c \"tools/**/*.js\" \"tests/**/*.js\" \"*.json\""
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-cli": "^5.0.0"
  },
  "prettier": {
    "printWidth": 110,
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  }
}
